# packages/ai-workflows

**Retired in phase 8.** The legacy `atlas-ui-skill` was removed; repo skills now live in `.agents/skills/`
(symlinked as `.claude/skills`): `atlas-prototype`, `atlas-component`, `atlas-verify`, `atlas-figma-sync`.
