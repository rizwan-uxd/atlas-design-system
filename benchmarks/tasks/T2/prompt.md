Build a new prototype screen called "settings" — a mobile Profile & Settings screen, like the other prototypes in this project.

It needs:
- A profile header with an avatar (photo or initials), name and email.
- A "Notifications" group with toggles for push, email and SMS, each with a short description.
- An "Appearance" group with a segmented choice for theme: Light / Dark / System.
- A destructive "Sign out" button that asks for confirmation in a dialog.

Use the Atlas design system. Register it so it appears on the prototypes index. When you're done, make sure it passes the token lint.
