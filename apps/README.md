# /apps

Runnable applications that consume packages from this monorepo.

| App | Description |
|-----|-------------|
| _(root)_ | **Atlas Web Sandbox** — Next.js 16 visual sandbox for all 12 Atlas/Web components. Run with `npm run dev` from repo root. |

> Future apps (Storybook, docs site, demo app) live here as `/apps/<name>/` with their own `package.json`.
