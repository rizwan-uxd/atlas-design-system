# docs/architecture

Per-component specification documents (`ATLAS-SPEC/`) and high-level architecture decisions.

Each file in `ATLAS-SPEC/` describes the full API, variants, states, and token usage for one Atlas component. Read these before implementing or modifying a component.
